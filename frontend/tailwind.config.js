/** @type {import('tailwindcss').Config} */
module.exports = {
  // mode: 'jit', //compile on fly, reduce build time
  // purge: ["./src/**/*.{html,ts}",], // purge unused styles
  content: ["./src/**/*.{html,ts}",],
  theme: {
    extend: {},
  },
  plugins: [],
}

